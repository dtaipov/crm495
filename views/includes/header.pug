include ./menu

nav(class="navbar navbar-toggleable-md navbar-light bg-faded")
  button(class="navbar-toggler navbar-toggler-right", type="button", data-toggle="collapse", data-target="#navbarSupportedContent", aria-controls="navbarSupportedContent", aria-expanded="false", aria-label="Toggle navigation")
    span(class="navbar-toggler-icon")
  a(class="navbar-brand", href="/documents") CRM495

  div(class="collapse navbar-collapse", id="navbarSupportedContent")
    ul(class="navbar-nav mr-auto")
      +menuItem('/documents', 'Документы')
      +menuItem('/finance_operations', 'Финансовые операции')
      +menuItem('/reports/products_balance', 'Склад')
    ul(class="nav navbar-nav ml-auto")
      li(class="nav-item")
        span(class="navbar-text") #{user.login}
      li(class="nav-item")
        a(href="/logout/", class="nav-link") Выйти